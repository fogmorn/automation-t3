apiVersion: v1
kind: Service
metadata:
  name: front1
  labels:
    name: front1
    component: frontend
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    name: front1
    component: frontend
